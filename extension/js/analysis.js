const updateVariableInAnalysis = (variable) => {
    // card = addHypothesisCard(variable.name, "hypothesis_dv" + variable.name);
    // card.on("click", function() {
    //     addListenertoHypothesisCard(card, variable);
    // });
}